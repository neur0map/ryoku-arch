# hypridle config , niri-adapted from the original Omarchy/Ryoku design.
# Replaces swayidle in Ryoku's services/Idle.qml (which is patched by
# install/config/ryoku-shell-branding.sh:apply_idle_disable_swayidle to
# skip its swayidle spawn).
#
# Why hypridle instead of swayidle:
#   - hypridle's `inhibit_sleep` is supposed to block suspend until the
#     lock surface is fully secure. On niri this requires the
#     hyprland-lock-notify-v1 protocol extension which niri does NOT yet
#     implement, so inhibit_sleep is currently a no-op here. We keep it
#     set so a future niri version will pick it up automatically.
#   - hypridle's lock_cmd is fired via logind's LockSession DBus signal
#     (universal , works on niri). Unlike Ryoku's embedded Lock.qml, the
#     ryoku-lock-qylock reads the Settings-selected SDDM Current= theme.
#     If that theme belongs to qylock, qylock's external Quickshell lockscreen
#     is used. Otherwise Ryoku falls back to hyprlock so suspend-driven locks
#     still present the default unlock UI.
#
# Lock surface choice , Settings-selected qylock themes for automated /
# suspend-driven locks; Ryoku's Lock.qml stays wired to Mod+Alt+L.

general {
    lock_cmd         = /bin/sh -c 'exec "$HOME/.local/share/ryoku/bin/ryoku-lock-qylock"'
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd  = niri msg action power-on-monitors
    inhibit_sleep    = 3
}

# 5 minutes idle: play the preserved terminal screensaver first. A later
# listener powers the monitors off if the machine remains idle.
listener {
    timeout    = 300
    on-timeout = /bin/sh -c 'exec "$HOME/.local/share/ryoku/bin/ryoku-launch-screensaver"'
    on-resume  = /bin/sh -c 'pkill -f org.ryoku.screensaver 2>/dev/null || true; niri msg action power-on-monitors'
}

# 6 minutes idle: turn screen off after the ASCII screensaver had time to play.
listener {
    timeout    = 360
    on-timeout = niri msg action power-off-monitors
    on-resume  = /bin/sh -c 'pkill -f org.ryoku.screensaver 2>/dev/null || true; niri msg action power-on-monitors'
}

# 10 minutes idle: lock the screen via DBus (which fires lock_cmd above)
listener {
    timeout    = 600
    on-timeout = loginctl lock-session
}

# 5.5 minutes idle: turn off keyboard backlight (restore on activity)
listener {
    timeout    = 330
    on-timeout = brightnessctl -sd '*::kbd_backlight' set 0
    on-resume  = brightnessctl -rd '*::kbd_backlight'
}
