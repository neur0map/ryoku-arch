-- Ryoku Hyprland palette. Regenerated from the active theme on every theme switch
-- (Settings > Color Scheme); the values below are only the fresh-install seed.
-- hyprland.lua reads these globals for its border/shadow colors, so re-theming
-- recolors the desktop without touching the rest of the config.
var_primary = "rgb(d0bcfe)"
var_tertiary = "rgb(efb8c7)"
var_surface_container = "rgb(2b292f)"
var_outline = "rgb(948f99)"
var_shadow = "rgba(00000099)"
