#? Config file for btop v. 1.4
# Ryoku owns this file; the palette theme is rendered by matugen (themes/ryoku.theme).

color_theme = "ryoku"

# keep the terminal's own background so btop sits on the shell's paper.
theme_background = False

truecolor = True

vim_keys = True
