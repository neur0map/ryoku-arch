# lets `pip install --user <pkg>` work against the externally-managed system
# python (PEP 668). without this pip refuses with "externally-managed-environment".
# user installs land in ~/.local (its bin/ already on PATH); system site-packages
# stay pacman-owned. for one-off CLI apps reach for pipx instead.
[global]
break-system-packages = true
