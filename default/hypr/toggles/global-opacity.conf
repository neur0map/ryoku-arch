# Apply a subtle global opacity override to normal application windows.
# Fullscreen windows stay opaque to avoid media and game rendering artifacts.
windowrule = opacity 0.90 override 0.80 override 1.0 override, match:class .*
