# The ISO ships iwd, not wpa_supplicant, so point NetworkManager's Wi-Fi backend
# at iwd. NetworkManager dbus-activates iwd on demand; the TUI then scans and
# joins networks through `nmcli dev wifi`.
[device]
wifi.backend=iwd
