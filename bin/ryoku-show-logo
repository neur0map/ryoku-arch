#!/bin/bash

# Display the Ryoku logo in the terminal using green color.
# Used by various presentation scripts to show branding.

source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)/lib/runtime-env.sh"

clear
echo -e "\033[32m"
cat "$RYOKU_PATH/logo.txt"
echo -e "\033[0m"
echo
