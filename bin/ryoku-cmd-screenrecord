#!/bin/bash

set -euo pipefail

source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)/lib/runtime-env.sh"

with_sound=false

for arg in "$@"; do
  case "$arg" in
  --with-desktop-audio|--with-microphone-audio)
    with_sound=true
    ;;
  --help)
    cat <<'USAGE'
Usage:
  ryoku-cmd-screenrecord [--with-desktop-audio] [--with-microphone-audio]

Starts iNiR's region recorder. Audio flags select iNiR's sound-capable
recording route.
USAGE
    exit 0
    ;;
  esac
done

if [[ $with_sound == "true" ]]; then
  exec inir region recordWithSound
fi

exec inir region record
