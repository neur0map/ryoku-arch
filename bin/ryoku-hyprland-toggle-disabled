#!/bin/bash

source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)/lib/runtime-env.sh"

# Check if a Hyprland toggle is currently disabled (missing).

[[ ! -f "$RYOKU_STATE_PATH/toggles/hypr/$1.conf" ]]
