#!/bin/bash

# Dismiss notifications after clicking an action.

if (($# == 0)); then
  echo "Usage: ryoku-notification-dismiss <summary>"
  exit 1
fi

inir notifications clearAll >/dev/null 2>&1 || true
