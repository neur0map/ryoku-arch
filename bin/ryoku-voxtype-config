#!/bin/bash
set -e
source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)/lib/runtime-env.sh"

# Used by Voxtype waybar module to open config on right click

exec ryoku-launch-editor ~/.config/voxtype/config.toml
