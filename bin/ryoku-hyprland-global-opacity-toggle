#!/bin/bash

source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)/lib/runtime-env.sh"

# Toggle subtle global transparency for normal application windows.

ryoku-hyprland-toggle \
  --enabled-notification "      Enable global window transparency" \
  --disabled-notification "      Disable global window transparency" \
  global-opacity
