#!/bin/bash

source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)/lib/runtime-env.sh"

# Historical name preserved so Hyprland bindings stay stable. Dispatches
# tofi for both dmenu and app-launcher modes.
#
#   --dmenu [...]   piped selection -> tofi --dmenu
#   otherwise       app launcher    -> tofi-drun
#
# Pre-cutover walker fallback was removed in Task 4.

if [[ ${1:-} == "--dmenu" ]]; then
  shift
  exec tofi --config "$RYOKU_PATH/default/tofi/config" "$@"
fi

exec tofi-drun --config "$RYOKU_PATH/default/tofi/config" "$@"
