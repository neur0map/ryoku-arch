#!/bin/bash

set -euo pipefail

source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)/lib/runtime-env.sh"

if [[ ${1:-} == "--dmenu" ]]; then
  shift
  exec fuzzel --dmenu "$@"
fi

exec inir overview toggle
