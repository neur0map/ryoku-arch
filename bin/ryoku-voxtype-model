#!/bin/bash
set -e
source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)/lib/runtime-env.sh"

ryoku-launch-floating-terminal-with-presentation "voxtype setup model"
ryoku-restart-shell >/dev/null 2>&1 || true
