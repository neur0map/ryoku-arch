#!/bin/bash

source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)/lib/runtime-env.sh"

# Launch the fastfetch TUI that gives information about the current system.

exec ryoku-launch-or-focus-tui "bash -c 'fastfetch; read -n 1 -s'"
