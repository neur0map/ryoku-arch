#!/bin/bash

source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)/lib/runtime-env.sh"

# Restart an application by killing it and relaunching it detached.
# Usage: ryoku-restart-app <application-name> [application-args...]

pkill -x "$1" || true
setsid "$@" >/dev/null 2>&1 &
