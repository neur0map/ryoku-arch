#!/bin/bash

source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)/lib/runtime-env.sh"

# Restart the XCompose input method service (fcitx5) to apply new compose key settings.

ryoku-restart-app fcitx5 --disable notificationitem
