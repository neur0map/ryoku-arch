# AMD GPUs with RADV drivers exhibit ghosting, flickering, and rendering
# glitches when QSG_RENDER_LOOP=threaded (QTBUG-113700).
# Use basic render loop to avoid these issues.

[Service]
Environment=QSG_RENDER_LOOP=basic
